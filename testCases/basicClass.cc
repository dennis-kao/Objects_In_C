#include <stdlib.h>

class A {
	int a, *b, **c;
	int d;
  char hello;
  int z;
  
  /*this is a comment */
  
  int first(signed char d, int c) {
    /*int d;*/
		char z;
		float lilBoat;
		return (b+z+c+d);
  }
  
  int div(signed char b, int d) {
		char f;
    return (a-b+c);
  }

	int add(char d) {
		/*int d;*/
		char z;
		float lilBoat;
		return (b+a+c+d);
	}
  
  int DIV2(char d, int c) {
    return (b / c);
  }
  
  int DIV3(char d, int c) {
    return (b    -    c);
  }
}

int main() {
	/* this is also a comment */
	//double slash comment lord save me!
 class A DEATHTOPONIES;
 int temp;
 int hello;
 
 DEATHTOPONIES.add(temp);
}
